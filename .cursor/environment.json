{
  "name": "TrnStudio · Горизонт",
  "install": "npm ci && npx playwright install chromium && python3 -m pip install --user -r worker/requirements-ci.txt",
  "terminals": [
    {
      "name": "studio",
      "command": "TRANSCRIBE_BACKEND=mock npm start"
    }
  ],
  "ports": [4173]
}
